# archive/

Code kept for historical/audit reference only. Nothing under this folder is built, run,
or referenced by the live app (`backend/`, `frontend/`), by root `docker-compose.yml`, or
by any CI. Do not add new features here — this is a read-only snapshot.
